@namespace AutoSzereloMuhely.Client.Layout

<div class="sidebar">
    

    <div class="@NavMenuCssClass pt-3">
        <nav class="nav flex-column">
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                    <i class="bi bi-menu-up me-2" ></i>  Kezdőlap
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="ugyfelek">
                    <i class="bi bi-file-earmark-person-fill me-2"></i> Ügyfeleink
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="munkak">
                    <i class="bi bi-hammer me-2"></i> Munkáink
                </NavLink>
            </div>
        </nav>
    </div>
</div>

@code {
    private bool collapseNavMenu = false;
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
